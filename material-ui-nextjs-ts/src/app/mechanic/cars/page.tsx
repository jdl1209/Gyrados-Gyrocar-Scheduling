'use client'

export default function Page() {
  // Add a new car field that permanently sits on the right side. Flexbox or grid for this.
  // Add car MUI Form (inside an Item) with 3 SelectFields and a Submit button.
  // Data Table to show active status of all cars.
  }