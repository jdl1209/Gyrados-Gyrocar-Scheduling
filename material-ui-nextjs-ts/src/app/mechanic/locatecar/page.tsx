'use client'

export default function Page() {
  // Let you click on pins to get a list of the available cars.
  // Do we just need a somewhat functional map? Sponsors no longer care if the car is actively showing on screen like Uber
  // Will likely need some semblance of pin placement. Could random number generate locations here. Will look into the dummy data
  // https://www.traccar.org/documentation/
  // Plus/Minus button to zoom in and out of map
  }