'use client'

import AdminDashboard from "@/components/AdminDashboard";
import CustomerDashboard from "@/components/CustomerDashboard";

//curently this is a blank page, might never be used, might set it up as a reference to another page as a fallback
//TODO: Set as reference to whichever page we want to be the default maybe?

// `app/page.tsx` is the UI for the `/` URL
export default function Page() {
    //return <CustomerDashboard></CustomerDashboard>
    //return <AdminDashboard></AdminDashboard>
  }